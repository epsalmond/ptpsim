[package]
name = "camera-sim"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
ptp-core = { workspace = true }
camera-config = { workspace = true }
camera-media-store = { workspace = true }
protocol-primitives = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
# The USB acceptance tests drive the FFI executor seams against the scripted
# USB responder, the mirror image of the BLE seam tests in camera-protocol-ffi
# (which dev-depend on camera-sim for the BleResponder).
camera-protocol-ffi = { workspace = true }
async-trait = { workspace = true }
futures = { workspace = true }
