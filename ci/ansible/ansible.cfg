[defaults]
inventory = inventory.yml
host_key_checking = False
stdout_callback = yaml
forks = 1
deprecation_warnings = True
retry_files_enabled = False

[ssh_connection]
# Pipelining cuts SSH round-trips for command/shell tasks. Safe with
# `requiretty` disabled in /etc/sudoers (default on macOS).
pipelining = True
