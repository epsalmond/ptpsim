[build]
rustc-wrapper = "scripts/rustc-wrapper"
# Cargo 1.97 applies this only to local packages, keeping dependency warnings
# visible without allowing them to break this workspace's builds.
warnings = "deny"
