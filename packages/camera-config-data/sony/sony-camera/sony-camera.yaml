schema: camera-config/v1

# PRELIMINARY placeholder body for "some Sony camera recognized over BLE".
# Recognition-only: the advert signature in ../index.yaml captures the
# advertised model code into runtime_scope (`sonyModelCode`); per-model
# bodies replace this placeholder once a real Sony camera is probed.
# Evidence: APK static pass only — see ../index.yaml header.
camera:
  manufacturer: SONY
  model: Sony camera (preliminary)

evidence:
  apkSonyBle:

    date: "2026-06-10"
